# Module: `emr-ec2-roles-aws`

IAM **instance profile** and **EC2 service role** for EMR worker nodes to assume (attach `AmazonElasticMapReduceforEC2Role` and your S3/KMS policies in the root module).
